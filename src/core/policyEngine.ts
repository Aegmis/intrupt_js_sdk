/**
 * Policy engine — placeholder, mirrors the empty `core/policy_engine.py`.
 *
 * `policies.yaml` at the repo root sketches a policy-engine concept, but no
 * policy is enforced anywhere today. This file exists so the JS layout matches
 * the Python one; wire real evaluation here (or, preferably, server-side) when
 * the policy engine is implemented.
 */
export {};
